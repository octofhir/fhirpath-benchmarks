pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,jsx,tsx}"
      root: "website/"
      run: npx biome check --write {staged_files}
    type-check:
      root: "website/"
      run: npm run type-check
      stage_fixed: true
